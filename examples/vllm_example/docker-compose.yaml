# Docker compose for vllm_example
# Extends the unified nCompass Docker configuration

services:
  vllm_example:
    extends:
      file: ./docker/docker-compose.base.yaml
      service: ncompass
    container_name: vllm_example
