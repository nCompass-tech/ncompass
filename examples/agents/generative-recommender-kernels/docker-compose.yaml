# Docker compose for generative-recommender-kernels
# Extends the unified nCompass Docker configuration

services:
  gr_kernels:
    extends:
      file: ./docker/docker-compose.base.yaml
      service: ncompass
