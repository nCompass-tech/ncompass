# Environment configuration for vLLM NCU/Nsys example
# Copy this file to env_config.yaml and fill in your values

# HuggingFace token (required for gated models)
HF_TOKEN: "your_huggingface_token_here"

# Optional: Directory for Torch profiler output
VLLM_TORCH_PROFILER_DIR: ".torch_traces"
